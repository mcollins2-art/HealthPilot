name: Build and Test

on:
  push:
    branches:
      - main
      - copilot/**
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: dotnet restore src/HealthPilot.Api/HealthPilot.Api.csproj

      - name: Build
        run: dotnet build src/HealthPilot.Api/HealthPilot.Api.csproj --configuration Release --no-restore --nologo

      - name: Test
        run: dotnet test tests/HealthPilot.Api.Tests/HealthPilot.Api.Tests.csproj --configuration Release --nologo
